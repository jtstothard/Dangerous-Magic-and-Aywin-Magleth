#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run an incremental wikilink validation for changed files only to keep pre-push fast
node scripts/validate-changed-wikilinks.mjs || (echo 'Wikilink validation failed for changed files' && false)
